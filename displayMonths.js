import {_myDate, _monthsOfTheYear, _calendarMonths, _currentDay, _currentMonth, _currentYear, _daysOfTheWeek} from  'Coding/Calendar/config.js';

let currentDate = myDate.toLocaleDateString('en-US');

document.getElementById("dateName").innerHTML = currentDate;

let 
